# DWTIS Phase 1 — Ingestion Pipeline
# Install with: pip install -r requirements_phase1.txt

httpx==0.27.0
feedparser==6.0.11
langdetect==1.0.9
praw==7.7.1          # Reddit — optional, falls back to JSON API without credentials
